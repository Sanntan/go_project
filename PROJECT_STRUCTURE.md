# 📁 Структура проекта

## Общая архитектура

Проект следует принципам **Clean Architecture** с разделением на слои:

```
internal/
├── api/              # API слой (REST, gRPC интерфейсы)
├── bootstrap/        # Инициализация и запуск сервисов
├── config/           # Конфигурация
├── fraud/            # Бизнес-логика анализа рисков
├── generator/       # Генераторы данных
├── grpc/             # gRPC сервер
├── kafka/            # Kafka producer/consumer
├── logger/           # Логирование событий
├── models/           # Модели данных
├── redis/            # Redis клиент (кэш)
├── services/         # Сервисный слой (бизнес-логика)
└── storage/          # Слой хранилища данных
```

## 🔍 Детальное описание

### 1. Storage (Хранилище данных)

**Расположение:** `internal/storage/`

**Структура:**
```
storage/
├── interfaces.go          # Интерфейс TransactionRepository
└── sqlite/                # Реализация для SQLite
    ├── connection.go      # ⚠️ ПОДКЛЮЧЕНИЕ К БД (NewConnection)
    ├── schema.go          # Инициализация схемы БД
    ├── repository.go      # Репозиторий (реализует интерфейс)
    ├── save.go            # CREATE операции
    ├── get.go             # READ операции
    ├── update.go          # UPDATE операции
    └── clear.go           # DELETE операции
```

**Важно:**
- **Подключение к БД** находится в `storage/sqlite/connection.go` (функция `NewConnection`)
- **CRUD операции** разделены по файлам для удобства
- **Интерфейс** определен в `storage/interfaces.go` для возможности замены реализации

**Пример использования:**
```go
// 1. Подключение к БД
storage, err := sqlite.NewConnection(cfg)

// 2. Создание репозитория
repo := sqlite.NewRepository(storage)

// 3. Использование через интерфейс
repo.SaveTransaction(...)
```

### 2. Redis (Кэш)

**Расположение:** `internal/redis/`

**Структура:**
```
redis/
├── client.go        # Redis клиент (конкретная реализация)
└── client_test.go   # Тесты
```

**Важно:**
- ✅ **Используется:** `internal/redis` - конкретная реализация Redis клиента
- ❌ **Удалено:** `internal/cache` - был дубликат, не использовался

**Почему только `redis`, а не `cache`?**
- `redis.Client` используется напрямую в коде (fraud, grpc, bootstrap)
- Интерфейс `cache.Cache` не использовался нигде
- Для будущей замены Redis можно создать интерфейс в `redis/interfaces.go`

### 3. Services (Сервисный слой)

**Расположение:** `internal/services/`

**Структура:**
```
services/
├── interfaces.go           # Интерфейсы сервисов
├── transaction_service.go  # Сервис транзакций
└── risk_analyzer.go       # Сервис анализа рисков
```

**Назначение:**
- Бизнес-логика приложения
- Использует интерфейсы storage, kafka, redis
- Не зависит от конкретных реализаций

### 4. API (API слой)

**Расположение:** `internal/api/`

**Структура:**
```
api/
├── interfaces.go    # Интерфейсы API обработчиков
└── rest/           # REST API
    ├── handlers.go # Обработчики запросов
    └── router.go  # Настройка маршрутов
```

### 5. Bootstrap (Инициализация)

**Расположение:** `internal/bootstrap/`

**Структура:**
```
bootstrap/
├── ingestion_service.go      # Инициализация Ingestion Service
└── fraud_detection_service.go # Инициализация Fraud Detection Service
```

**Назначение:**
- Создание всех зависимостей
- Подключение к БД, Redis, Kafka
- Запуск серверов (HTTP, gRPC)
- Graceful shutdown

## 🔄 Поток данных

```
1. Запрос → API (rest/handlers.go)
2. API → Services (transaction_service.go)
3. Services → Storage (storage/sqlite/repository.go)
4. Services → Kafka (kafka/producer.go)
5. Kafka → Services (fraud_detection_service.go)
6. Services → Redis (redis/client.go) для кэширования
```

## 📝 Рекомендации по структуре

### ✅ Правильно:
- Подключения к БД в `storage/sqlite/connection.go`
- CRUD операции разделены по файлам
- Интерфейсы определены для возможности замены реализации
- Одна реализация Redis в `redis/`

### ❌ Избегать:
- Дублирования функционала (cache и redis)
- Смешивания слоев (storage не должен знать о services)
- Прямых зависимостей от конкретных реализаций

## 🚀 Будущие улучшения

Если понадобится заменить Redis на другой кэш:

1. Создать `redis/interfaces.go` с интерфейсом `Cache`
2. Обновить `redis/client.go` чтобы реализовывал интерфейс
3. Создать новую реализацию (например, `cache/memory.go`)
4. Заменить в bootstrap

Если понадобится добавить PostgreSQL:

1. Создать `storage/postgres/` аналогично `storage/sqlite/`
2. Реализовать интерфейс `TransactionRepository`
3. Заменить в bootstrap: `postgres.NewConnection()` вместо `sqlite.NewConnection()`

