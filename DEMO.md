# Демонстрация работы системы

Краткое руководство для демонстрации всех возможностей системы.

## 🚀 Быстрый запуск

### 1. Запуск инфраструктуры

```powershell
docker-compose up -d
```

Дождитесь, пока все контейнеры станут "healthy" (1-2 минуты).

### 2. Запуск сервисов

**Терминал 1 - Ingestion Service:**
```powershell
go run cmd/ingestion-service/main.go
```

**Терминал 2 - Fraud Detection Service:**
```powershell
go run cmd/fraud-detection-service/main.go
```

**Терминал 3 - Фронтенд (опционально):**
```powershell
cd frontend
npm run dev
```

## 📖 Демонстрация возможностей

### 1. Swagger UI - REST API документация

**Откройте в браузере:**
```
http://localhost:8080/swagger/index.html
```

**Что показать:**
- ✅ Список всех API endpoints
- ✅ Интерактивное тестирование API
- ✅ Схемы запросов и ответов
- ✅ Попробуйте endpoint `GET /api/v1/transactions/generate`

**Команда для быстрого теста:**
```powershell
# Генерация случайной транзакции через API
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/transactions/generate"
```

### 2. gRPC - Высокопроизводительный API

**Список доступных сервисов:**
```powershell
grpcurl -plaintext localhost:50051 list
```

**Генерация случайной транзакции:**
```powershell
grpcurl -plaintext -d '{}' localhost:50051 transaction.TransactionService/GenerateRandomTransaction
```

**Анализ транзакции:**
```powershell
grpcurl -plaintext -d '{
  "transaction_id": "TXN-DEMO-001",
  "account_number": "ACC123456789",
  "amount": 2500000.0,
  "currency": "RUB",
  "transaction_type": "international_transfer",
  "counterparty_country": "KY",
  "channel": "online",
  "timestamp": "2024-01-15T14:30:00Z"
}' localhost:50051 transaction.TransactionService/AnalyzeTransaction
```

**Получение статуса транзакции:**
```powershell
grpcurl -plaintext -d '{"processing_id": "proc_ваш-id"}' localhost:50051 transaction.TransactionService/GetTransactionStatus
```

### 3. Веб-интерфейс

**Откройте в браузере:**
```
http://localhost:3000
```

**Что показать:**
- ✅ Статус сервисов (онлайн/офлайн)
- ✅ Форма отправки транзакций
- ✅ Автогенерация случайных данных
- ✅ Список транзакций с цветовой индикацией риска
- ✅ Детальная информация о транзакциях
- ✅ Логи и аналитика

### 4. REST API - Примеры запросов

**Отправка транзакции:**
```powershell
$body = @{
    transaction_id = "TXN-DEMO-001"
    account_number = "ACC123456789"
    amount = 1500000.0
    currency = "RUB"
    transaction_type = "international_transfer"
    counterparty_country = "KY"
    channel = "online"
    timestamp = "2024-01-15T14:30:00Z"
} | ConvertTo-Json

$response = Invoke-RestMethod -Uri "http://localhost:8080/api/v1/transactions" `
    -Method Post `
    -ContentType "application/json" `
    -Body $body

Write-Host "Processing ID: $($response.processing_id)"
```

**Проверка статуса:**
```powershell
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/transactions/$($response.processing_id)"
```

**Получение всех транзакций:**
```powershell
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/transactions?limit=10"
```

## 🎯 Сценарий демонстрации

### Сценарий 1: Полный цикл обработки транзакции

1. **Откройте Swagger UI** → `GET /api/v1/transactions/generate` → Получите случайную транзакцию
2. **Скопируйте данные** и отправьте через `POST /api/v1/transactions`
3. **Получите processing_id** из ответа
4. **Проверьте статус** через `GET /api/v1/transactions/{processing_id}`
5. **Через несколько секунд** статус изменится на "reviewed" с оценкой риска

### Сценарий 2: Сравнение REST и gRPC

1. **REST API** - Отправьте транзакцию через Swagger UI
2. **gRPC** - Отправьте ту же транзакцию через grpcurl
3. **Покажите**, что оба метода работают с одной системой

### Сценарий 3: Анализ рисков

1. **Низкий риск** - Сумма < 500k, обычная страна, дневное время
2. **Средний риск** - Сумма 500k-1M + офшор ИЛИ крупная сумма + ночное время
3. **Высокий риск** - Крупная сумма + офшор + ночное время

## 📊 Что показать в системе

- ✅ **Микросервисная архитектура** - два независимых сервиса
- ✅ **Асинхронная обработка** - Kafka для очередей
- ✅ **Кэширование** - Redis для быстрого доступа
- ✅ **REST API** - Swagger документация
- ✅ **gRPC** - Высокопроизводительный протокол
- ✅ **Веб-интерфейс** - Современный UI на Vue.js
- ✅ **Анализ рисков** - Множество правил для оценки транзакций

## 🔍 Проверка работы системы

**Health checks:**
```powershell
# Ingestion Service
Invoke-RestMethod -Uri "http://localhost:8080/health"

# Fraud Detection Service
Invoke-RestMethod -Uri "http://localhost:8081/health"
```

**Статистика:**
```powershell
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/stats"
```

**События:**
```powershell
Invoke-RestMethod -Uri "http://localhost:8080/api/v1/events?limit=10"
```

## ⚡ Быстрые команды

```powershell
# Проверка всех сервисов
docker-compose ps

# Логи Kafka
docker-compose logs kafka

# Логи Redis
docker-compose logs redis

# Остановка всего
docker-compose down
```

## 📝 Примечания

- Все сервисы должны быть запущены для полной функциональности
- Swagger доступен только после генерации документации (`swag init`)
- gRPC требует Redis для работы
- Фронтенд автоматически обновляется при изменении данных

